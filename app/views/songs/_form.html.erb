<%= form_for @song do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :length %>
  <%= f.number_field :length %>

  <%= f.label :year %>
  <%= f.select :year, (1500..1950).to_a, :include_blank => {:year => "Select year"} %>

  <%= f.label :artist_id %>
  <%= f.collection_select :artist_id, Artist.all, :id, :last_name %>

  <%= f.submit (@song.new_record? ? "Compose" : "Update") %>
<% end %>
